{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([anduril_lattice.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'anduril_lattice.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
